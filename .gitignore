
# Local-only project handoff docs
beatthescam-project-handoff.md
beatthescam_security_handoff.md
.DS_Store
.env

# Python bytecode cache
__pycache__/
*.pyc

# Node deps for the Netlify Functions' package.json (never committed)
node_modules/

# Google OAuth credentials - NEVER commit these
client_secret*.json
token.json
google_token.json

# Video production handoff (local only)
beatthescam-video-handoff.md
.claude/

# Local video render output — never shipped to Netlify or git
/out/
/dist/videos/

# Video music bed — local render asset only (licensed; never commit or deploy)
assets/audio/

# Gate-quarantined drafts — failed the accuracy gate; local review only, never published
content/quarantine/

# Editorial review working-docs — local-only QA artifacts (readable draft + operator
# -c.md fact-checks for the diversification batches). Regenerable from posts.json; never
# site content, so kept out of the repo. Removed from tracking 2026-06-28.
docs/review/
.release-journal.json
