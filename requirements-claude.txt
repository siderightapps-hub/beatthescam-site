# Upper bounds cap surprise major upgrades; Dependabot (.github/dependabot.yml)
# proposes bumps within range. Pinned 2026-06-22 (supply-chain audit fix).
anthropic>=0.120.2,<1.0.0
Pillow>=12.3.0,<13.0.0

# Needed by scripts/tweet_new_articles.py (called by tweet-on-publish.yml
# once a review PR merges to main — the content crons themselves no longer
# tweet). Added 2026-05-25 when the tweet step was silently failing with
# "tweepy not installed".
tweepy>=4.17.0,<5.0.0
python-dotenv>=1.2.2,<2.0.0

# Asset minifiers used by scripts/build.py to shrink dist/assets/styles.css
# and dist/assets/app.js. Added 2026-05-30 to clear the "unminified JS/CSS"
# Semrush warnings (1 source file each, but Semrush counts them on every
# page = ~192 warnings cleared for two tiny pure-Python wheels). Build
# falls back gracefully if either is missing (prints a skip message).
rcssmin>=1.2.2,<2.0.0
rjsmin>=1.2.5,<2.0.0
